import NavBar from "../NavBar/NavBar";

const Header = () => {
    return (
        <header>
            <h1>TCM Logistic</h1>
            <NavBar />
        </header>
    );
};

export default Header;